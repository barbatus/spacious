query GetCharacters($page: Int, $pageSize: Int) {
  characters(page: $page, pageSize: $pageSize) {
    pagination {
      total
    }
    nodes {
      id
      name
      pictureUrl
      friendsCount
    }
  }
}

