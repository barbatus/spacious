query GetPlanets($page: Int, $pageSize: Int) {
  planets(page: $page, pageSize: $pageSize) {
    pagination {
      total
    }
    nodes {
      id
      name
      pictureUrl
      population
    }
  }
}

